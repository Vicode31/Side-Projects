import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Calendrier semaines de vie",
  description:
    "Calendrier permettant de visualiser la vie d'un humain en semaines. Et de voir combien de semaines il nous reste à vivre pour profiter de la vie intensément.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
